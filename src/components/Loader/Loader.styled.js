import styled from 'styled-components';

const LoaderStyledContainer = styled.div`
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 3;
`;

export { LoaderStyledContainer };
